Register the current working directory as a claude-switch profile.

Run: `claude-switch register`

This adds the current directory to `~/.config/claude-switch/config.yaml` so it appears in the `cs` TUI selector.

If the user provides a name argument like `/login myproject`, use: `claude-switch register myproject`
